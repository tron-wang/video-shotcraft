node_modules/
out/qa/
template/out/
# 发帖用社交封面图等本地素材，不进 skill 安装包
assets/images/
# 样片 mp4 存 gallery-media release，不进 git（poster 小图仍在 git）；
# 本地预览用 gallery/fetch-media.sh 拉取
gallery/media/*.mp4
# Showcase 用户投稿媒体与清单存 showcase-media release，不进 git
gallery/showcase-media/
gallery/api/showcase.json
# 本地教程视频
/video-shotcraft教程.mp4
.DS_Store
.synapse/
__pycache__/
CLAUDE.md
AGENT.md
PRODUCT.md
/shots/

# motion-lab 本地研究/渲染农场（动效已转换为正式镜头卡，此目录不进库）
/motion-lab/
.playwright-mcp/

# 自架部署的主機設定（見 assets/scripts/deploy-gallery.sh），不進公開 repo
gallery/.deploy.env

# 模板區的成片範例（影片、封面、templates.json）是使用者自己的作品，只隨自架部署上傳，不進公開 repo
gallery/templates/

# 口播模式的 API 金钥（MiniMax / Pexels / Pixabay / Unsplash），绝不进库
.env

# 使用者本機设定（成片工程根目录等），不进 git
shotcraft.local.json
